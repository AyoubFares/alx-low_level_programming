#include <stdio.h>

/**
 * main - prints alphabet in lower case
 *
 * Return: Always 0 (success)
*/

int main(void)
{

	char ch = 'a';

	while (ch <= z)
	{
		if ( ch == 'q' || ch == 'e')
		putchar(ch);
		ch++;
	}
		putchar('\n');
	return (0);
}
